/* Mongo DB client */

class MongoProxy {

    // Stub method for testing
    echo(msg) {
        return new Promise( (resolve, reject) => {
            resolve(msg);
        })
    }
}

module.exports = MongoProxy;